<html lang="en">

  <head>
    <meta charset="UTF-8" />
    <title>BookingUni</title>
    <link rel="stylesheet" href="/static/css/style.css" />
  </head>

  <body>

    <div id="container">
      {{#if error}}
      <div>
            <div class="notification loadingBox">
                Loading …
            </div>
            <div class="notification infoBox">
                <span>Info</span>
            </div>
            <div class="notification errorBox">
                <span>{{error}}</span>
            </div>
        </div>
        {{/if}}
      <nav>
        <div class="left-container">
          <ul>
            <li><a href="/">Home</a></li>
          {{#if user.isAuthenticated}}

            <li><a href="/hotels/add">Add +</a></li>
          {{else}}
            <li><a href="/users/login">Login</a></li>
            <li><a href="/users/register">Register</a></li>

          </ul>
          {{/if}}
        </div>
        {{#if user.isAuthenticated}}
        <div class="right-container">
          <a href="/users/logout" class="log-out">{{user.username}}</a>
          <a href="/users/logout" class="log-out">Logout</a>
        </div>
          {{/if}}
      </nav>
      {{{body}}}

      <footer>@SoftUni - JS Back-end - BookingUni 2020</footer>
    </div>

  </body>

</html>